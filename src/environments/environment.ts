// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.



/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.


export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCcbsKKbV5PPgbyTNp39a3hMG5wC8oUM5A",
    authDomain: "recordando-la-5465f.firebaseapp.com",
    databaseURL: "https://recordando-la-5465f.firebaseio.com",
    projectId: "recordando-la-5465f",
    storageBucket: "recordando-la-5465f.appspot.com",
    messagingSenderId: "986107731856"
  }
};